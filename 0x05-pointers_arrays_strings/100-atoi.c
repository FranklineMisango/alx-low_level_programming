#include "holberton.h"

/**
 * _atoi - Converts string to integer
 * @s - the string
 * @i - the index of a value in the string
 * Return: i or 1
 */

int _atoi(char *s)
{
  *s = str;
  int i = 0;

  while (i != '\0')
    {
      if (i <= 9 && i >= 0)
	{
	  putchar(str[i])
	    }
      i++;
    }
  else
    {
      return (0);
    }
}
