CC = gcc
SRC = main.c school.c
OBJ = *.o
NAME = school
RM = rm -f

all: ${OBJ}
	    ${CC} ${OBJ} -o ${NAME}

clean: 
		${RM} -f *~ ${NAME}

oclean:
		${RM} -f ${OBJ}

fclean:
		${RM} -f *~ ${NAME} ${OBJ}

re: oclean ${OBJ}
		${CC} ${OBJ} -o ${NAME}
