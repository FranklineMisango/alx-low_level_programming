#include "holberton.h"

/**
 * _strlen_recursion - returns the length of a string
 * @s: the string
 * Return: 0 or i
 */

int _strlen_recursion(char *s)
{
int i = 0;
if (s[i] == '\0')
return (0);
{
i = i + 1;
i++;
return (i);
}
}
