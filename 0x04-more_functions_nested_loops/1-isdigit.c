#include "holberton.h"

/**
 * _is digit - checks if c is a digit
 * Description - reurn 1 if digit and 0 otherwise
 * @c: integer to be tested
 * Return - 1 or 0
*/

int _isdigit(int c)
if (c >= 0 && c <= 9)
{
return (1);
}
else
{
return (0);
}
