#include "holberton.h"

/**
 * *malloc_checked - allocates memory using malloc
 * @b: unsigned int
 * Return: pointer to b or 98
*/

void *malloc_checked(unsigned int b)
{
int *a = malloc(b);
if (a == 0)
exit(98);

return (a);
}
